# Lambda layer dependencies. boto3 is omitted because the Lambda runtime
# provides it; test-only packages are installed by the workflows, not shipped.
# zoneinfo's IANA data. The Lambda image may not ship /usr/share/zoneinfo, and
# a missing zone would make every late-ice deadline fail to compute.
tzdata==2026.4
